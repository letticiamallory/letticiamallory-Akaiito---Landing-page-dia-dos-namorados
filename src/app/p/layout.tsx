import { StudioFontsShell } from "@/components/studio-fonts-shell";

export default function PresenteLayout({ children }: { children: React.ReactNode }) {
  return <StudioFontsShell>{children}</StudioFontsShell>;
}
