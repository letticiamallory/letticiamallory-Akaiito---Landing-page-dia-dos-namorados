# SVGs do museu com WebP embedado (1–4 MB cada) — Git LFS evita limite de 100 MB no push
public/museum/*.svg filter=lfs diff=lfs merge=lfs -text

# Vídeo demo do presente
public/assets/demo/*.webm filter=lfs diff=lfs merge=lfs -text
